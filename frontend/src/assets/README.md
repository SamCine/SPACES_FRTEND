# assets/

Static assets imported by the app (images, icons, fonts, lottie/animation
files, etc.). Place files used via JS imports here, e.g.:

```js
import logo from "@/assets/logo.svg";
```

Files that must be referenced by absolute URL (favicons, manifest) belong in
`/public` instead.

This placeholder keeps the directory tracked in version control until real
assets are added.
